# Spec Generator

The **Generator** allows you to quickly create new files with the correct Metadata Header.

## Usage
1.  Select a **Category** (e.g., Character).
2.  Enter a **Title**.
3.  (Optional) Enter version and description.
4.  **Generate**: The tool will create the `.md` file in the correct folder, ensuring it is indexed by the system.
